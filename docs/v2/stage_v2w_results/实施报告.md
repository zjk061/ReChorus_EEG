# 阶段 V2-W 决策支持包

## 1. 执行结论

V2-W 已生成数据/任务/协议调整的用户决策支持包。该阶段不改变任务定义，不训练模型，不访问 `v2_locked_legacy_test`，只把 V2-F 到 V2-V 的证据整理成可选择方向。

- 当前 deployable 最佳 `margin_power0p2_base0p025`，protocol GAUC `0.681370`。
- 已测试同类 dev-only fold/seed oracle GAUC `0.691426`，距离 `0.8` 仍差 `0.108574`。
- same-family model repair exhausted: `True`。
- 当前必须等待用户选择：`USER_DECISION_data_task_protocol_adjustment`。

## 2. 可批准方向

| priority | option | type | evidence | requires approval |
|---:|---|---|---|---:|
| 1 | `approve_session_local_task_redefinition` | `task_definition_change` | medium | True |
| 2 | `approve_context_valid_sampling_protocol` | `protocol_sampling_change` | high | True |
| 3 | `approve_label_quality_or_data_collection_revision` | `data_label_change` | high | True |
| 4 | `do_not_change_protocol_freeze_current_dev_best` | `freeze_without_0p8` | high | True |
| 5 | `approve_new_eeg_signal_or_feature_acquisition` | `feature_or_data_acquisition_change` | medium | True |

## 3. 推荐默认选择

若目标仍是尽量冲击 `0.8+`，推荐优先批准 `approve_session_local_task_redefinition`，并配套 `approve_context_valid_sampling_protocol`。原因是现有证据显示 EEG 在用户/会话内排序任务中更有表达空间，但当前样本有效 pair context 仍不足，继续同类模型小修很难弥补这个上限。

## 4. 明确禁止

未得到用户批准前，不应执行任务定义变化、指标合同变化、训练/评估采样协议变化，也不得访问 locked test。
