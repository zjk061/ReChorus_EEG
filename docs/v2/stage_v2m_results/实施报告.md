# 阶段 V2-M 实施报告

## 1. 执行结论

V2-M 已根据用户批准完成 two-stage user-local EEG reranker 协议冻结。该阶段只读取 rolling train/dev 和 V2-L 产物，未访问 `v2_locked_legacy_test`。

- 冻结协议：`V2M-two_stage_user_local_reranker-v1`。
- 批准记录：`2026-07-07`，`User approved formal shift to two-stage user-local reranker / same-user local ranking protocol.`
- 选中候选：`V2H-local-real` / `v2j_same_user_local_ranking_metric`。
- 选中指标范围：`same-user local pair/list ranking`。
- 新协议下 GAUC `0.675698`，Macro User AUC `0.678074`，第三 AUC 口径 `0.675698`。
- 相对原 rolling-dev 正式冠军 GAUC 差值 `+0.059677`。
- 是否达到 `0.8`：`False`；是否可进入 locked test：`False`。
- 下一阶段：`V2-N_protocolized_reranker_improvement`。

## 2. 冻结后的任务定义

新的正式开发主线不再把 EEG ranker 解释为跨用户全局概率模型，而是解释为两阶段推荐中的同用户个性化重排序器：

1. 第一阶段 rolling-like/global candidate provider 产生候选集合或基础分数。
2. 第二阶段真实 EEG reranker 只在同一用户、同一 session 或同一候选集合内部重排。
3. EEG reranker 不允许跨用户直接重排，不允许使用当前 event EEG/MAES/label，不允许使用 future 或 locked test 统计。

## 3. 指标合同

| rank | metric | current best | definition |
|---:|---|---:|---|
| 1 | `protocol_gauc` | 0.675698 | Weighted user-level AUC under the active protocol. For event transfer this is rolling-dev GAUC; for local reranking this is same-user local user-pair GAUC. |
| 2 | `protocol_macro_user_auc` | 0.678074 | Unweighted mean of valid user AUC values under the active protocol. |
| 3 | `global_auc_risk` | 0.587175 | Cross-user event-level AUC. It is recorded as a score-comparability risk, not as a veto against a valid same-user reranker improvement. |

## 4. 候选资格审计

| candidate | protocol | eligible | GAUC | Macro User AUC | Global AUC | seed win rate | interpretation |
|---|---|---:|---:|---:|---:|---:|---|
| `V2H-local-real` | `v2j_same_user_local_ranking_metric` | True | 0.675698 | 0.678074 | 0.675698 | 1.000 | `new_protocol_candidate` |
| `V2H-local-real` | `v2j_train_only_user_centered_transfer` | True | 0.667919 | 0.668260 | 0.587175 | 1.000 | `new_protocol_candidate` |
| `V2K-real-full_mean_std-same_session` | `v2k_protocolized_same_session_reranker` | True | 0.662471 | 0.664766 | 0.593207 | 1.000 | `new_protocol_candidate` |
| `V1-profile-dynamic-gated_cross_attention` | `current_formal_rolling_dev_champion` | False | 0.616020 | 0.619353 | 0.654840 | 1.000 | `original_reference_only` |

## 5. F1/locked test 准备状态

- 可继续新协议 dev 迭代：`True`。
- 可进入 F1 冻结审计：`True`。
- 可访问 locked test：`False`。
- locked test 前阻塞项：`target_0p8_reached, locked_test_approval_available`。

V2-M 的结论不是“已经达到 0.8”，而是“新协议已经获批并冻结，可以在该协议下继续做真实 EEG reranker 的 AUC 提升”。下一步应进入 V2-N，在不访问 locked test 的前提下继续提升 protocol GAUC / Macro User AUC。
